# Results

這裡存放已整理好的 benchmark 成果，而不是開發過程中所有零散輸出。

目前分成兩條主線：

- [`single_route`](single_route): 單一路徑版本，用來說明純 SQA、warm start、block move 的差異。
- [`alternative_route`](alternative_route): 目前主線，對齊 kSPwLO 的雙路徑替代路線題目。

每個結果資料夾都盡量保留：

- manifest
- summary CSV / JSON
- benchmark overview 圖
- 必要時的代表性 run 輸出

# Alternative-route Results

## English

This is the current main result line of the repository.

The problem definition has already moved beyond a single route:

- fixed source and target
- find `k = 2` routes
- constrain route overlap

This is the pedestrian-routing version aligned with the kSPwLO direction.

Two main result packages are included:

- [`hard_theta_formal_02`](hard_theta_formal_02): the formal benchmark with hard-theta / overlap-budget control
- [`mode_compare_formal_01`](mode_compare_formal_01): comparison across warm-start and overlap-constraint modes

## 中文

這一層是目前 repo 的主線成果。

問題定義已經不再只是單一路徑，而是：

- 固定 source 與 target
- 求 `k = 2` 條路
- 控制兩條路之間的 overlap

也就是與 kSPwLO 方向對齊的 pedestrian alternative routing。

目前包含兩套主要成果：

- [`hard_theta_formal_02`](hard_theta_formal_02): 使用 hard-theta / overlap-budget 控制的正式 benchmark
- [`mode_compare_formal_01`](mode_compare_formal_01): 比較 warm-start 與 overlap-constraint 模式的成果

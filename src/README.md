# Source Code

這裡放的是可執行的研究程式碼。

目前主要內容是：

- [`sqa_algorithm`](sqa_algorithm): SQA 引擎、routing rules、benchmark 工具與執行入口。

如果只想知道真正要跑哪裡，直接從 [`sqa_algorithm/run.py`](sqa_algorithm/run.py) 和 [`sqa_algorithm/tools`](sqa_algorithm/tools) 開始即可。
